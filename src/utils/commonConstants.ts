
import { sql } from "drizzle-orm";

export const MODE: string = "global" // Change this value to 'global' or 'local'
// export const MODE: string = "local"; // Change this value to 'global' or 'local'